{
  "version": 2,
  "builds": [
    {
      "src": "geojson-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    },
    {
      "src": "geojson-backend/src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "geojson-frontend/build/$1"
    },
    {
      "src": "/api/(.*)",
      "dest": "geojson-backend/dist/$1"
    }
  ]
}
